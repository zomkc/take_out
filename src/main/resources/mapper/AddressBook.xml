<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.mapper.AddressBookMapper">
    <insert id="SaveBook">
        INSERT INTO address_book ( user_id, consignee, phone,sex,detail,label )
        VALUES
        ( #{userId}, #{consignee},#{phone},#{sex},#{detail},#{label} );
    </insert>
</mapper>

    
